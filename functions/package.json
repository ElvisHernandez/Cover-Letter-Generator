{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc --watch",
    "serve": "concurrently \"npm run build:watch\" \"firebase emulators:start --only functions,database,storage,auth --import ./private/emulator_data --export-on-exit ./private/emulator_data\"",
    "shell": "concurrently \"npm run build:watch\" \"firebase functions:shell\"",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/src/index.js",
  "dependencies": {
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1",
    "openai": "^4.11.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/pdf-parse": "^1.1.2",
    "concurrently": "^8.2.1",
    "firebase-functions-test": "^3.1.0",
    "typescript": "^5.2.2"
  },
  "private": true
}
